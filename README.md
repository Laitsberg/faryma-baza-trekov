# База треков Faryma

Витрина архива разносов композитора и страницы отдельных треков — одной
автономной HTML-страницей. Ни сборки, ни сервера, ни зависимостей: файл
открывается двойным кликом и работает целиком в браузере.

**`baza-trekov.html`** — это и есть вся страница. Внутри неё лежат стили,
скрипты, разбор строки архива (той же логикой, что и статистика), библиотека
разбора CSV и сама таблица разносов срезом на день сборки.

## Что умеет

**Витрина.** Карточки с обложкой, оценкой композитора и номером эфира. Поиск,
сортировка, фильтры по оценке, жанру, году эфира, стране, происхождению, типу и
тэгам — с живыми счётчиками у каждого пункта. Случайный трек, своя заявка
«предложить трек», подгрузка по 60 карточек.

**Страница трека** (`#t=<ключ>`). Открывается кликом по карточке, живёт по
своему адресу — ссылку можно переслать, работает кнопка «назад». Обложка,
теги, обе оценки, кнопки «слушать» и «смотреть разнос», переход к соседям в
текущем отборе. Вкладки: встроенный плеер эфира на минуте разбора, вся строка
таблицы целиком, похожие треки по исполнителю и жанру. Сбоку — своя оценка по
шкале, быстрые факты и распределение оценок исполнителя.

**Своё, в браузере зрителя.** Сердечко, своя оценка, добавленные треки и
предложенные обложки лежат в `localStorage` и видны только тому, кто их
поставил: чисел лайков и зрительских оценок в архиве нет.

## Как обновлять

**Таблица разносов.** Содержимое `<script id="csvData">` — выгрузка листа
как есть, вместе со строками-разделителями эфиров (`СТРИМ №271 (22.08.26)`).
Заменяется целиком; ничего пересчитывать не надо, страница разбирает её при
каждом открытии.

**Обложки.** У части архива ссылки на трек нет, и превью взять неоткуда —
там зритель может предложить свою картинку со страницы трека. Заявка живёт в
его браузере и ждёт утверждения. Чтобы утвердить: открыть файл с `#covers`
в конце адреса, сложить туда присланные строчки, скопировать готовый блок и
заменить им `COVERS_INLINE` в файле.

**Страны исполнителей.** `COUNTRIES_INLINE` — справочник «имя → страна» с
оценкой уверенности; ниже `COUNTRY_MIN_SCORE` страна не показывается.

## Чего здесь нет

Сервера, авторизации и общих данных. Всё, что делает зритель, остаётся в его
браузере, а утверждение обложек ручное: одна вкладка не может ничего
рассказать другой, и файл у каждого свой. Для общих лайков, зрительских оценок
и модерации прямо на сайте понадобится база с проверкой прав — это отдельный
разговор.
